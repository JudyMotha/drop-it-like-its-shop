// This index to help render everything in the order we need them to render in
// const router = require('express').Router();
// const apiRoutes = require('./api');

// router.use('/api', apiRoutes);

// module.exports = router;
